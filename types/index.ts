export interface StoryCardProps {
    number: string;
    title: string;
    imgUrl: string;
}

export interface CategoryProps {
    imgUrl: string;
    location: string;
    like: string;
}

export interface DestinationProps {
    imgUrl: string;
    location: string;
    duration: string;
    price: string;
}